<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.rag.mapper.SystemConfigMapper">

    <resultMap id="BaseResultMap" type="com.campus.rag.entity.SystemConfig">
        <id column="id" property="id"/>
        <result column="config_key" property="configKey"/>
        <result column="config_value" property="configValue"/>
        <result column="description" property="description"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM system_config ORDER BY id
    </select>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT * FROM system_config WHERE config_key = #{configKey}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO system_config (config_key, config_value, description, create_time, update_time)
        VALUES (#{configKey}, #{configValue}, #{description}, NOW(), NOW())
    </insert>

    <update id="updateByKey">
        UPDATE system_config
        SET config_value = #{configValue}, description = #{description}, update_time = NOW()
        WHERE config_key = #{configKey}
    </update>
</mapper>
