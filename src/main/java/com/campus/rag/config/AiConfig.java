package com.campus.rag.config;

import org.springframework.context.annotation.Configuration;

/**
 * AI 配置（如 LangChain4j 等自定义配置）
 */
@Configuration
public class AiConfig {
    // 后续可在此配置 LangChain4j、大模型 API 等
}
